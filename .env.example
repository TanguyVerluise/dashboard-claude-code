# Neon Postgres (Vercel Marketplace) — chaîne de connexion poolée
DATABASE_URL=postgresql://user:password@ep-xxx.eu-central-1.aws.neon.tech/neondb?sslmode=require

# Secret partagé pour authentifier le webhook MemberPress
# Utilisé soit en query (?token=...) soit en header (x-webhook-secret)
WEBHOOK_SECRET=change-me-long-random-string
